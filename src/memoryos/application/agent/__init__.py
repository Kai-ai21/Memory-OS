"""Phase 7: the corpus as tools an agent can call.

M7.0 is scaffolding and nothing else. One model turn, at most one tool call, one
answer — no planning, no chaining, no memory between turns. What it proves is
that the tools work and that the descriptions route.
"""
